//node js is a c++ program that provide run time environment for java script, 
//with node we can do much things like accessing a file etc,
//node js uses LIBUV library, and Google V8 engine
//In node there is no window like in browser
//in node we have global which have all the apis

//Node is set to be server side platform based on asynchronous IO
//that is non blocking, it means it uses javascript out side of the browser
//and ceates the runtime environment out side of the browser